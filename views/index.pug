extends layout

block content
  //- h1= title
  //- p Welcome to #{title}
  .container
    p(class='sticky') This is a placeholder
    p(class='sticky weather') The weather is currently #{cur_temp}
    p(class='sticky calendar') Calendar is not set up
