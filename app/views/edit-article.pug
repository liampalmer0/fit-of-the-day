extends layout

block content
  include includes/internal-nav
  .message
    if error
      p.error Error: Article data unavailable
  main.editArticle(role='main').card
    h1 Edit Article
    .articleForm
      include includes/article-form
